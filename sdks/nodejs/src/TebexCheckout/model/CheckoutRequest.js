/**
 * Tebex Checkout API
 * The Checkout APIs are designed to allow our creators to use the Tebex Checkout flow and payment acceptance capabilities without the need to set up a Tebex-powered webstore. Using these APIs allows you to create baskets with custom products (as opposed to pre-created products on our webstore platform), and send customers directly to the checkout flow to proceed with payment options.  You must receive prior authorisation before the Checkout API is enabled on your account. Please contact customer support or your account manager to discover if you qualify to use the Checkout API before beginning integration.
 *
 * The version of the OpenAPI document: 1.1.2
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CheckoutItem from './CheckoutItem';
import CheckoutRequestBasket from './CheckoutRequestBasket';
import Sale from './Sale';

/**
 * The CheckoutRequest model module.
 * @module TebexCheckout/model/CheckoutRequest
 * @version 1.1.2
 */
class CheckoutRequest {
    /**
     * Constructs a new <code>CheckoutRequest</code>.
     * @alias module:TebexCheckout/model/CheckoutRequest
     * @param basket {module:TebexCheckout/model/CheckoutRequestBasket} 
     * @param items {Array.<module:TebexCheckout/model/CheckoutItem>} An array of `Packages` in the basket.
     */
    constructor(basket, items) { 
        
        CheckoutRequest.initialize(this, basket, items);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, basket, items) { 
        obj['basket'] = basket;
        obj['items'] = items;
    }

    /**
     * Constructs a <code>CheckoutRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexCheckout/model/CheckoutRequest} obj Optional instance to populate.
     * @return {module:TebexCheckout/model/CheckoutRequest} The populated <code>CheckoutRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CheckoutRequest();

            if (data.hasOwnProperty('basket')) {
                obj['basket'] = CheckoutRequestBasket.constructFromObject(data['basket']);
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [CheckoutItem]);
            }
            if (data.hasOwnProperty('sale')) {
                obj['sale'] = Sale.constructFromObject(data['sale']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CheckoutRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CheckoutRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CheckoutRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `basket`
        if (data['basket']) { // data not null
          CheckoutRequestBasket.validateJSON(data['basket']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                CheckoutItem.validateJSON(item);
            };
        }
        // validate the optional field `sale`
        if (data['sale']) { // data not null
          Sale.validateJSON(data['sale']);
        }

        return true;
    }


}

CheckoutRequest.RequiredProperties = ["basket", "items"];

/**
 * @member {module:TebexCheckout/model/CheckoutRequestBasket} basket
 */
CheckoutRequest.prototype['basket'] = undefined;

/**
 * An array of `Packages` in the basket.
 * @member {Array.<module:TebexCheckout/model/CheckoutItem>} items
 */
CheckoutRequest.prototype['items'] = undefined;

/**
 * @member {module:TebexCheckout/model/Sale} sale
 */
CheckoutRequest.prototype['sale'] = undefined;






export default CheckoutRequest;

